CREATE TABLE users (id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY NOT NULL, username VARCHAR(32) UNIQUE NOT NULL, password VARCHAR(64) NOT NULL, passwordhash VARCHAR(64) NOT NULL);

CREATE TABLE history(id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY NOT NULL, user_id INT UNSIGNED NOT NULL, query VARCHAR(2048) NOT NULL, INDEX(user_id));

